"""The practice domain: tempo, scheduling and the practice session.

Pure Python and pydantic models. Nothing here opens a file, reads the clock or
touches the global `random` — a `now` and a `random.Random` are passed in.
"""
