"""The browser front end: FastAPI, Jinja2 and HTMX over `domain/`.

Server-rendered fragments, no Node, no CDN: `static/htmx.min.js` is vendored
because the whole point of leaving the spreadsheet was to practise with the
network off.
"""
